
<%= line_chart @earnings_over_time %>
<%= line_chart @subscriptions_over_time %>

<%= line_chart [
	{name: "Posts", data: @posts_over_time },
	{name: "Comments", data: @comments_over_time }
	]
%>

<%= column_chart [
	{name: "Accepted", data: @number_charges_accepted },
	{name: "Declined", data: @number_charges_declined }
	],
	stacked: true
%>

<%= column_chart [
	{name: "Accepted", data: User.group_by_day(:created_at).count },
	{name: "Declined", data: Comment.group_by_day(:created_at).count }
	],
	stacked: true
%>

<%= line_chart @amount_charges_declined %>
<%= line_chart @amount_charges_accepted %>






<%= line_chart User.group_by_day(:created_at).count %>

<%= line_chart @posts_over_time %>

<%= line_chart Post.where(project_id: @project.id).group_by_month(:created_at, last: 6).count %>

<%= line_chart [
  {name: "Users", data: User.group_by_day(:created_at).count},
  {name: "Comments", data: Comment.group_by_day(:created_at).count},
	{name: "Posts", data: Post.group_by_day(:created_at).count}
] %>
